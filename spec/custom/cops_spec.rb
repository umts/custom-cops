require 'spec_helper'

describe Custom::Cops do
  it 'has a version number' do
    expect(Custom::Cops::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
